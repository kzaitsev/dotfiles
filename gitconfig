[core]
	excludesfile = /Users/bugagazavr/.gitignore_global
	editor = nvim -f
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[user]
	email = kirik910@gmail.com
	name = Kirill Zaitsev
[alias]
	permission-reset = !git diff -p -R | grep -E \"^(diff|(old|new) mode)\" | git apply
[init]
	templatedir = ~/.git_template
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
